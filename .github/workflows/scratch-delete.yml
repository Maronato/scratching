name: Delete PR Scratch Org
on:
  pull_request:
    types: [closed]
    # branches:
    #   - "scratch-alpha/**"

jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Get PR number
        uses: actions/github-script@v6
        id: get_issue_number
        with:
          script: |
            if (context.issue.number) {
              // Return issue number if present
              return context.issue.number;
            } else {
              // Otherwise return issue number from commit
              return (
                await github.rest.repos.listPullRequestsAssociatedWithCommit({
                  commit_sha: context.sha,
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                })
              ).data[0].number;
            }
          result-encoding: string
      - name: Get scratch org alias
        id: get_org_alias
        run: echo "alias=pull-request-${{ steps.get_issue_number.outputs.result }}" >> "$GITHUB_OUTPUT"

      - name: Install sfdx
        run: npm install @salesforce/cli@2.9.8 --global

      - name: Authenticate
        run: |
          echo "${secrets.SFDX_AUTH_FILE}" > auth.json
          sf org login sfdx-url --sfdx-url-file auth.json --set-default-dev-hub --set-default --alias devhub
          rm auth.json

      - name: Query existing PR scratch org
        id: query_current_org
        env:
          PR_NUMBER: ${{ steps.get_issue_number.outputs.result }}
        run: sf org display --target-org=pull-request-${PR_NUMBER} --json >> "$GITHUB_OUTPUT"

      - name: Delete existing PR scratch org
        # Do nothing if the scratch org doesn't exist
        if: fromJson(steps.query_current_org.outputs).status == 0
        run: sf org delete scratch --target-org=${{ steps.get_org_alias.outputs.alias }} --no-prompt
